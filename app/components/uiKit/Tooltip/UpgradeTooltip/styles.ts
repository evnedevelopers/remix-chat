import { Theme } from '@mui/material/styles';

export const styles = {
  upgradeTooltip: (theme: Theme) => ({
    borderRadius: '10px',
    background: theme.palette.background.paper,
    padding: '10px 15px',
  }),
};
